<%= current_user.username %>
Goals:
Private Goals
<% @private_goals.each do |goal| %>
  <%= goal.title %>
  <%= goal.description %>
  <%= "status: #{goal.status}" %>
<% end %>

Public Goals
<% @public_goals.each do |goal| %>
  <%= goal.title %>
  <%= goal.description %>
  <%= "status: #{goal.status}" %>
<% end %>
